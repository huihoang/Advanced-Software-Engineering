export const summary = {
  create: (field: string) => `Create ${field.toLowerCase()}`,
  update: (field: string) => `Update ${field.toLowerCase()}`,
  required: (field: string) => `${field} is required`,
};
